import { YIELD_ADAPTER } from './yield.js';

// NOTE: Yield's and Illuminate's adapters are the same, we re-export Yield's adapter as Illuminate's adapter.

export { YIELD_ADAPTER as ILLUMINATE_ADAPTER };
